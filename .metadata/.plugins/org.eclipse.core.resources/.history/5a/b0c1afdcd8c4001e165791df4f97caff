package main.java.gestiondocument;

public class GestionnaireDocumentPDF {
	public void stockerPDF(Document document) {
        // Logique spécifique pour stocker un document PDF
        System.out.println("Stockage du document PDF : " + document);
    }

    public void convertirHTMLenPDF(Document document) {
        // Logique spécifique pour convertir un document HTML en PDF
        System.out.println("Conversion du document HTML en PDF : " + document);
    }

    public void rechercherPDF(String requete) {
        // Logique spécifique pour rechercher des documents PDF
        System.out.println("Recherche du document PDF avec la requête : " + public void stockerPDF(String document) {
            // Logique spécifique pour stocker un document PDF
            System.out.println("Stockage du document PDF : " + document);
        }

        public void convertirHTMLenPDF(String document) {
            // Logique spécifique pour convertir un document HTML en PDF
            System.out.println("Conversion du document HTML en PDF : " + document);
        }

        public void rechercherPDF(String requete) {
            // Logique spécifique pour rechercher des documents PDF
            System.out.println("Recherche du document PDF avec la requête : " + requete);
        });
    }
}
